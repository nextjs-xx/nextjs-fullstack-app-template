{
  "name": "gateway",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "node ./src/index.js",
    "rover": "rover supergraph compose --config ./supergraph-config.yaml > supergraph.graphql",
    "start": "npm run rover && npm run serve"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@apollo/gateway": "^2.0.1",
    "@apollo/rover": "^0.7.0",
    "apollo-server": "^3.6.7",
    "dotenv": "^16.0.0",
    "graphql": "^16.4.0"
  }
}
